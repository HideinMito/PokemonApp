<?xml version="1.0" encoding="utf-8"?>
<!-- res/drawable/button_shape.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Forma del botón (rectangular con bordes redondeados) -->
    <solid android:color="@color/colorButton"/> <!-- Color de fondo -->
    <corners android:radius="16dp"/> <!-- Bordes redondeados -->
    <stroke android:color="@color/colorBorder" android:width="2dp"/> <!-- Borde -->
</shape>
